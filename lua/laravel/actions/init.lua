---@class laravel.actions.action
---@field check fun(self, bufnr: number): Promise
---@field format fun(self, bufnr: number): string
---@field run fun(self, bufnr: number): Promise

return {
    "laravel.actions.go_to_migration",
    "laravel.actions.open_env",
    "laravel.actions.add_relation",
}
